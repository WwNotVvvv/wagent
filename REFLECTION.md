# 反思报告

本项目使用 OpenCode 和 Superpowers 完成了一个 Go 语言的 Coding Agent Harness。回顾整个过程，我认为 Superpowers 最有价值的地方不是替我生成代码，而是把模糊想法拆成 SPEC、PLAN、实现、测试和收尾验证几个阶段。brainstorming 帮助我明确了治理是主要贡献方向，writing-plans 把功能拆成可执行任务，TDD 和 finishing-a-development-branch 则提供了检查生成代码的依据。相比只向 Agent 提出“帮我写一个工具”，这种流程更容易发现遗漏，也更适合形成可审计的工程记录。

TDD 对本项目总体上是放大器，但执行成本也很明显。测试先行迫使我把“危险命令应被拒绝”“验证失败后应改变下一步动作”“API Key 不应进入子进程”这些要求写成可观察行为，而不是停留在提示词层面。另一方面，原始实现中仍有测试过于宽松的问题：反馈闭环演示只运行了循环并记录错误，没有断言 Agent 是否真的改变动作。独立审查后，我增加了可注入的 Verifier 和严格的 `done → 失败反馈 → read_file → done` 断言。由此可见，测试数量并不等于测试质量，测试必须证明关键机制的因果关系。

subagent-driven 工作流提高了实现速度，但也暴露了任务边界和验收标准的重要性。一个任务最好只承担一个职责，并明确输入、输出和失败测试；否则 Agent 可能报告“完成”，但遗漏文件提交、错误处理或关键断言。SPEC 和 PLAN 的质量直接影响结果。例如，早期方案写了 Verifier 反馈闭环，却没有把“下一步动作必须变化”写成可执行断言，最终实现就容易退化成“测试运行过了”。

本项目的主要工程收获来自治理设计。命令 allow/ask/deny、路径范围限制、HITL、Trace 和 API Key 脱敏都由 Go 代码实现，并可以脱离真实 LLM 进行确定性测试。凭据与分发要求也迫使我处理了容易忽略的问题：Go 版本声明曾与 CI 不一致；Memory 笔记、Trace 任务边界和 Verifier 子进程环境曾存在安全缺口；README 还需要补充真实的安装、配置和 Release 说明。

过程中也有明显的流程问题。项目始终在 main 分支开发，没有使用 feature branch 或 worktree；OpenCode 曾把“项目目录发生变化”的通知误解成 SPEC 已获批准并提前进入 PLAN；它还曾把存在未提交文件的工作区报告为 clean。这些经历说明，人工必须明确区分信息同步、意见反馈和阶段授权，并独立检查 `git status`、提交内容和测试结果，不能只接受 Agent 的总结。

在协作分工上，AI 主要负责根据已经确认的 SPEC 和 PLAN 编写代码、补充测试、运行检查并生成文档初稿；我负责确定项目范围、选择治理方案、判断风险、确认设计、实际运行程序并审阅结果。一个有效的 Prompt/context 不只是描述“修复问题”，还要给出可观察的行为和验收条件。例如，真实 LLM 读取 README 后返回了 Markdown，导致 JSON 解析失败。我没有只要求提高 `max_steps`，而是明确要求把解析错误回灌给 Agent，限制重试次数，并增加“read_file 成功 → 非法 Markdown → 错误反馈 → done”的回归测试。这个过程让我认识到，Prompt 的价值在于补充约束和验收标准，而不是替代代码机制。

本项目实际没有完成作业要求的冷启动验证。开发过程中虽然使用了多个 subagent，但它们都处于已有项目上下文中，并不是只获得 SPEC.md 和 PLAN.md 的陌生 Agent，因此不能把这些 session 当作冷启动证据。

CI 也提供了一个很具体的教训。第一次 GitHub Actions 运行在 Ubuntu 上失败，原因不是 Go 版本或 Actions 配置，而是测试代码把 `cmd.exe` 写死，Linux 环境无法执行。根据日志定位后，我将测试命令改为按平台选择 Windows 的 `cmd.exe` 或 Unix 的 `sh`，并让同一个 `unit-test` job 同时覆盖 Windows 和 Ubuntu，最终两个平台的测试与 `go vet` 都通过。这次经历说明，声明“跨平台”不等于真正具备跨平台证据，平台差异必须进入测试设计。

本次补充使用 AI 辅助整理项目记录、检查作业要求覆盖情况和润色表达；其中的项目决策、流程偏差、测试失败和改进结论均来自真实开发记录。提交前我需要逐段核对事实，并按照自己的实际理解和语言重新确认，避免把 AI 生成的概括误当成个人经历。

如果重新开始，我会在实现前创建隔离分支，先让第二个 Agent 只依据 SPEC 和 PLAN 做冷启动审查，再开始 subagent 实现；每个任务完成后立即核对 diff、测试和提交；最后预留专门时间完成 CI、Release、过程日志和反思，而不是把它们当作收尾时的附属工作。对 Agent 最有效的提示也应包含明确的 JSON/接口格式、验收断言、禁止事项和遇到不确定情况时暂停询问的规则。
