# SPEC 设计过程记录

## 2026-08-05：项目位置变更导致阶段确认歧义

### 事件

在 SPEC 审阅过程中，我调整了 `wagent` 项目的目录位置，并向 OpenCode 通知了这一变化。该消息的本意只是同步工作目录变化，并不表示我已经确认 SPEC，也不表示允许进入下一阶段。

OpenCode 将这次位置变更误解为“SPEC 没有意见”，因此提前开始了 `PLAN.md` 的生成流程。

### 影响

- SPEC 审阅与 PLAN 开始之间的确认门槛被跳过。
- 工作阶段从 SPEC 审阅提前切换到了计划阶段。
- 暴露出“上下文变更通知”和“阶段确认指令”在对话中缺少明确区分的问题。

### 原因分析

这不是 SPEC 内容本身的问题，而是人机协作中的状态同步问题。消息同时包含了项目位置变化这一事实，但没有明确标注“仅通知，不代表确认”。Agent 根据上下文推断出了超出消息本意的授权。

### 反思与改进

1. 路径、文件位置、环境等上下文变化，应明确写成“仅同步信息，不代表对当前文档的审阅确认，也不授权进入下一阶段”。
2. 设计阶段的状态转换必须使用明确的确认语句，例如“确认 SPEC，可以进入 PLAN”。
3. Agent 在进入下一阶段前，应复述当前阶段、目标文件和获得的确认依据；若没有明确确认，应暂停并询问。
4. 后续过程记录中区分“事实通知”“意见反馈”和“阶段授权”，避免把沉默或上下文变化当作批准。

### 经验

在使用 Coding Agent 进行长流程开发时，文件位置变化、工具切换和环境变化都可能被 Agent 当作流程信号。人类需要显式表达状态和授权边界，Agent 也应对阶段转换设置确认检查点。

